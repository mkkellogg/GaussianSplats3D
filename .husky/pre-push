git stash
npx rollup -c && git add ./build && npm run fix-js && git add src && git commit --amend --no-verify --no-edit
EXIT=$?
if [ "$EXIT" != 0 ]
then
 git reset --hard HEAD
fi
git stash pop || true;
exit $EXIT